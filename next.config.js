/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: false,
    images: {
      domains: ['i.ibb.co', 'transit-club.com', 'm.media-amazon.com']
    }
}

module.exports = nextConfig
